import * as React from "react"

const Spinner = () => {
    return (
        <div className="flex justify-center items-center h-screen">
            <Spinner size={48} color="blue" /> {/* Adjust size and color as needed */}
        </div>
    );
};

export default Spinner;
